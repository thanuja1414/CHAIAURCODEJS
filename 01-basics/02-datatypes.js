"use strict"; //treat all JS code as newer version

// alert("hello") -> can't be run in nodejs for now , but there is a special syntax so we can use alert in local machine rather than in browser.

let name = 'tanuja'
let age = 22
let isLoggedIn = false
let state = null

//primitive datatypes
// number -> 2 to the power 53
// big int
// bool -> true / false
// null -> represents an empty value , intentionally leaving a variable empty (not undefined) -> standalone value 
// undefined -> variable is declared but yet not assigned with a value.
// symbol -> used to find uniqueness
// object

console.log(typeof age)
console.log(typeof null) // object
console.log(typeof undefined) // undefined

